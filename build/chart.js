// Generated by CoffeeScript 1.11.1
(function() {
  var firebaseRef, svg, svgH, svgH2, svgW, svgW2, svgl, svgr, updateMFChart, xMargin, yMargin;

  firebaseRef = firebase.database().ref('/items');

  firebaseRef.orderByChild('gender').once('value').then(function(snapshot) {
    var i, j, mf_numbers, pyramid_f, pyramid_m, ref;
    mf_numbers = [
      {
        'gender': 'male',
        'value': 0
      }, {
        'gender': 'female',
        'value': 0
      }
    ];
    pyramid_m = [];
    pyramid_f = [];
    for (i = j = ref = 100 - 1; j >= 0; i = j += -1) {
      pyramid_m.push({
        'age': i + 1,
        value: 0
      });
      pyramid_f.push({
        'age': i + 1,
        value: 0
      });
    }
    snapshot.forEach(function(child) {
      var index, item, k, l, len, len1;
      if (child.val()['gender'] === 'Male') {
        mf_numbers[0]['value'] += 1;
        for (index = k = 0, len = pyramid_m.length; k < len; index = ++k) {
          item = pyramid_m[index];
          if (item['age'] === child.val()['age']) {
            pyramid_m[index]['value'] += 1;
          }
        }
        return console.log(child.val());
      } else if (child.val()['gender'] === 'Female') {
        mf_numbers[1]['value'] += 1;
        for (l = 0, len1 = pyramid_f.length; l < len1; l++) {
          item = pyramid_f[l];
          if (item['age'] === child.val()['age']) {
            item['value'] = +1;
          }
        }
        return console.log(child.val()['age']);
      }
    });
    console.log(pyramid_m);
    return updateMFChart(mf_numbers);
  });

  svgW = 300;

  svgH = 200;

  svgW2 = 300;

  svgH2 = 200;

  yMargin = 50;

  xMargin = 60;

  updateMFChart = function(dataSet) {
    var barchart, scale, xAxis, xAxisCall;
    console.log('##updatechart');
    console.log(dataSet);
    scale = d3.scale.linear().domain([
      0, d3.max(dataSet, function(d) {
        return d.value;
      })
    ]).range([0, svgH]);
    barchart = svg.selectAll('rect').data(dataSet).enter().append('rect').attr({
      x: xMargin,
      y: function(d, i) {
        return i * 30 + yMargin;
      },
      width: function(d) {
        return scale(d.value);
      },
      height: 20,
      fill: "blue"
    });
    xAxisCall = d3.svg.axis().scale(scale).orient('bottom');
    xAxis = svg.append('g').attr({
      "class": "axis",
      "transform": "translate(" + [xMargin, 0] + ")"
    }).call(xAxisCall);
    svg.append("text").attr("x", 10).attr("y", 65).text("男性");
    return svg.append("text").attr("x", 10).attr("y", 95).text("女性");
  };

  svg = d3.select("body").append("svg").attr("width", svgW).attr("height", svgH);

  d3.select("body").append("div");

  svgl = d3.select("body").append("svg").attr("width", svgW2).attr("height", svgH2).style('background-color', 'gray');

  svgr = d3.select("body").append("svg").attr("width", svgW2).attr("height", svgH2).style('background-color', 'red');

}).call(this);
